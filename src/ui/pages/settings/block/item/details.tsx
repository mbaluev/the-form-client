import { Box } from '@mui/material';
import { Form } from '@ui/pages/settings/block/item/form';

export const Details = () => {
  return (
    <Box paddingLeft={3} paddingRight={3} overflow="auto">
      <Form />
    </Box>
  );
};
